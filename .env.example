# Database (Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hackathon"

# Moru API — get your key at https://moru.io/dashboard
# Used by the web app (sandbox creation) and agent template builds
MORU_API_KEY="moru_..."

# Anthropic API — https://console.anthropic.com/
# Embedded in agent template via .credentials.json
ANTHROPIC_API_KEY="sk-ant-..."

# Base URL for sandbox callbacks
# Local dev: use ngrok (https://ngrok.com) to expose localhost
BASE_URL="http://localhost:3000"

# YouTube Data API
YOUTUBE_API_KEY="your-youtube-api-key"
